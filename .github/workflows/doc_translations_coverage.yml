name: Doc Translations Coverage

on:
    workflow_dispatch:
    push:
        branches:
          - master

jobs:
    call-workflow:
        #temp untill merge
        uses: yparitcher/doc/.github/workflows/docs.yml@gh-pages
        #uses: koreader/doc/.github/workflows/docs.yml@gh-pages

