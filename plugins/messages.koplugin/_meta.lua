local _ = require("gettext")
return {
    name = "messages",
    fullname = _("Messages"),
    description = _([[Adds an action to gestures and profiles to show a message with patterns support.]]),
}
